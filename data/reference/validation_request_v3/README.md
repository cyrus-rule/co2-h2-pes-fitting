# Frozen validation request v3

These coordinates are public/versioned project evidence; they contain no new
ab initio labels. `manifest.json` records their hashes, generation parameters,
input data identity, evidence boundary, counts, and preregistered decision rule.

Do not edit the CSV files in place. A changed request needs a new artifact ID
and must be frozen before inspecting the corresponding electronic-structure
energies.
