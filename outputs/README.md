# Generated outputs

Use `outputs/local/` for unreviewed runs; it is ignored by Git. Commit only small,
group-approved artifacts under `outputs/reference/`, always with their manifest.
Do not commit private raw energies or derived data without confirming release
permission.
